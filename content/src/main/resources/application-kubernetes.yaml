server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://discovery/eureka/

spring:
  profiles:
    active: kubernetes
  rabbitmq:
    host: ${RABBITMQ_URI}
    port: 5672
    username: ${RABBITMQ_USER}
    password: ${RABBITMQ_PASSWORD}
